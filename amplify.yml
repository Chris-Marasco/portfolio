version: 1
frontend:
  phases:
    build:
      commands: ["yarn install && ls && yarn build"]
    test:
      commands: ["yarn test"]
  artifacts:
    baseDirectory: "./build"
    files:
      - '**/*'
  cache:
    paths: ["/node_modules/**"]
